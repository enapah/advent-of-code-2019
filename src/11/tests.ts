import {assert} from 'chai';

import {part1, part2} from './solution';

describe('Day 11', () => {
  describe('part 1', () => {
    it('solution', () => {
      assert.equal(part1(), 2082);
    });
  });

  describe('part 2', () => {
    it('solution', () => {
      console.log(part2());
    });
  });
});
