export enum Direction {
  Up,
  Right,
  Down,
  Left
}

export enum Mode {
  Color,
  Move
}

export interface Position {
  x: number;
  y: number;
}

export enum TurnDirection {
  Left,
  Right
}

export enum Color {
  Black,
  White
}
