import {assert} from 'chai';

import {part1, part2} from './solution';

describe('Day 17', () => {
  describe('part 1', () => {
    it('examples', () => {

    });

    it('solution', () => {
      assert.equal(part1(), 9876);
    });
  });

  describe('part 2', () => {
    it('examples', () => {});

    it('solution', () => {
      part2();
    });
  });
});
