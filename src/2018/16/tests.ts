import {assert} from 'chai';

import {part1, part2} from './solution';

describe('Day XX', () => {
  describe('part 1', () => {
    it('examples', () => {});

    it('solution', () => {
      assert.equal(part1(), 500);
    });
  });

  describe('part 2', () => {
    it('examples', () => {});

    it('solution', () => {
      part2();
    });
  });
});
