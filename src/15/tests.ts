import {assert} from 'chai';

import {part1, part2} from './solution';

describe('Day 15', () => {
  describe('part 1', () => {
    it('examples', () => {});

    it('solution', () => {
      assert.equal(part1(), 216);
    });
  });

  xdescribe('part 2', () => {
    it('examples', () => {});

    it('solution', () => {
      part2();
    });
  });
});
