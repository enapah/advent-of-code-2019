import {assert} from 'chai';

import {part1, part2} from './solution';

describe('Day XX', () => {
  describe('part 1', () => {
    it('examples', () => {
      assert.equal(part1(), 464);
    });

    it('solution', () => {});
  });

  describe('part 2', () => {
    it('solution', () => {
      assert.equal(part2(), 652);
    });
  });
});
